import { style } from '@vanilla-extract/css';

export const commentDetail = style({
  display: 'flex',
  flexDirection: 'column',
});
export const commentSummary = style({
  fontSize: '15px',
});

export const commentInfo = style({
  color: '#828282',
  marginRight: '5px',
});
